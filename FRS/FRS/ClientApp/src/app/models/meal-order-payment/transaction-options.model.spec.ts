import { TransactionOptions } from './transaction-options.model';

describe('TransactionOptions', () => {
  it('should create an instance', () => {
    expect(new TransactionOptions()).toBeTruthy();
  });
});
